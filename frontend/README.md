# Hyperproof Frontend

React + TypeScript UI for residual risk management.

## Scripts

```bash
npm install
npm run dev
npm run build
```

API base URL defaults to `http://localhost:3001`. Override with `VITE_API_BASE_URL`.
